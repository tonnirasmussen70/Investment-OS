name: Apply Editable Settings

on:
  push:
    paths:
      - "update_editable_settings.py"
      - ".github/workflows/apply-editable-settings.yml"

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Update app.py
        run: python update_editable_settings.py
      - name: Check syntax
        run: python -m py_compile app.py
      - name: Commit changes
        run: |
          rm -f update_editable_settings.py
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add app.py update_editable_settings.py
          git diff --cached --quiet || git commit -m "Make model settings and momentum weights editable"
          git push
