name: Apply Yahoo Position Price

on:
  push:
    paths:
      - 'update_yahoo_position_price.py'
      - '.github/workflows/apply-yahoo-position-price.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Apply update
        run: python update_yahoo_position_price.py
      - name: Syntax check
        run: python -m py_compile app.py
      - name: Commit changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add app.py
          git diff --cached --quiet || git commit -m "Use Yahoo price in Positioner"
          git push
